import Link from 'next/link';
import { can, propertyTypeLabel, type PropertyType, type ProjectRole } from '@property/domain';
export function ProjectHeading({ project, active }: { project: { id: string; name: string; role: ProjectRole; status: string; propertyType: PropertyType }; active: 'details' | 'site' | 'requirements' | 'history' | 'access' }) {
  return <><Link className="back-link" href="/projects">← All projects</Link><div className="page-heading"><div><p className="eyebrow">{propertyTypeLabel(project.propertyType).toUpperCase()}</p><h1>{project.name}</h1><p className="intro">Your property’s permanent project record.</p></div><span className={`status ${project.status === 'ARCHIVED' ? 'muted' : ''}`}>{project.status === 'ACTIVE' ? 'Active project' : 'Archived project'}</span></div>
    <nav className="project-tabs" aria-label="Project navigation"><Link aria-current={active === 'details' ? 'page' : undefined} href={`/projects/${project.id}`}>Project details</Link>{can(project.role, 'site:read') && <Link aria-current={active === 'site' ? 'page' : undefined} href={`/projects/${project.id}/site`}>Land &amp; site</Link>}{can(project.role, 'project:edit') && <Link aria-current={active === 'requirements' ? 'page' : undefined} href={`/projects/${project.id}/requirements`}>Requirements</Link>}{can(project.role, 'versions:read') && <Link aria-current={active === 'history' ? 'page' : undefined} href={`/projects/${project.id}/history`}>Version history</Link>}{can(project.role, 'members:manage') && <Link aria-current={active === 'access' ? 'page' : undefined} href={`/projects/${project.id}/access`}>Access</Link>}</nav></>;
}
